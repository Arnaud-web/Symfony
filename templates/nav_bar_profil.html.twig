{% if app.user %}

<div class="container">
    <nav class="navbar navbar-expand-sm bg-secondary navbar-dark"
        style="padding-top: 2px; border :2px solid white; font-size:13px ; max-height:40px ">

        <div class="navbar-header">
            <a class="navbar-brand"  href=" {{ path('blog_profil',{'id':app.user.id} ) }} ">My Article <span class="btn btn-success" style="border-radius: 50%;">
                  {% if numberArticle %}    {{ numberArticle}} {% endif %} 
             </span> </a>
        </div>
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('blog_create',{'id':app.user.id})}}">Create Article</a>
                </li>
                 {# <li class="nav-item">
                    <a class="nav-link" href=" {{ path('blog_profil',{'id':app.user.id} ) }} ">My Articles</a>
                </li> #}
                <li class="nav-item"><a class="nav-link"
                    href=" {{ path('user_profil',{'id':app.user.id}) }} ">{{app.user.username}}</a> </li>
                <li class="nav-item">
                    <a class="nav-link" href=" {{ path('security_modif', {'id': app.user.id }) }} ">Modifier mon Profil</a>
                </li>

            </ul>
            <ul class="navbar-nav">
                <li class="nav-item"> <a class="nav-link" href="{{ path('security_logout') }}">Deconnection</a> </li>
            </ul>
        </div>
    </nav>
</div>

{% endif %}